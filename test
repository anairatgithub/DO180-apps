#!/bin/sh


tmux -f /home/ba.ad.ssa.gov/405135/work/tmux/tmux.conf att -t director || \
      tmux -f /home/ba.ad.ssa.gov/405135/work/tmux/tmux.conf new -s director \; \
      new-window -n Dev \; \
         send-keys 'set -o vi' C-m 'while true ; do printf "\0"; sleep 240; done &' C-m \; \
      new-window -n Prod_NSC \; \
         send-keys 'set -o vi' C-m 'while true ; do printf "\0"; sleep 240; done &' C-m  \; \
      new-window -n Prod_SSC \; \
         send-keys 'set -o vi' C-m 'while true ; do printf "\0"; sleep 240; done &' C-m  \; \
      new-window -n Test \; \
         send-keys 'set -o vi' C-m 'while true ; do printf "\0"; sleep 240; done &' C-m  \; \
      select-window -t Dev
      
#new-window -n nsc-prd-osp-director-01.is.cl.ssa.gov "ssh -i /home/ba.ad.ssa.gov/405135/.ssh/id_rsa 405135@ba.ad.ssa.gov@nsc-prd-osp-director-01.is.cl.ssa.gov" \; \
#send-keys 'set -o vi' C-m 'while true ; do printf "\0"; sleep 240; done &' C-m 'sudo -i' C-m \; \
